import { Text, View } from "react-native";

export default function Index() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      
      <Text>Scan the qr code using the camera app instead of the expo go app. It works then only</Text>
    </View>
  );
}
